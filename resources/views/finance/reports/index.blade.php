@extends('layouts.finance')
@section('header', 'Finance Reports')
@section('content')
<div class="card p-6">@php($payments = \App\Models\FeePayment::with('learner')->latest()->paginate(25))<h2 class="text-xl font-bold text-gray-800 mb-5">Payment report</h2><div class="overflow-x-auto"><table class="min-w-full divide-y divide-gray-200"><thead><tr><th class="px-3 py-2 text-left text-xs uppercase">Receipt</th><th class="px-3 py-2 text-left text-xs uppercase">Learner</th><th class="px-3 py-2 text-right text-xs uppercase">Amount</th><th class="px-3 py-2 text-left text-xs uppercase">Method</th><th class="px-3 py-2 text-left text-xs uppercase">Date</th></tr></thead><tbody class="divide-y">@forelse($payments as $payment)<tr><td class="px-3 py-2 text-sm">{{ $payment->receipt_number }}</td><td class="px-3 py-2 text-sm">{{ $payment->learner?->full_name }}</td><td class="px-3 py-2 text-right text-sm">KES {{ number_format($payment->amount,2) }}</td><td class="px-3 py-2 text-sm">{{ ucfirst($payment->payment_method?->value ?? $payment->payment_method) }}</td><td class="px-3 py-2 text-sm">{{ $payment->paid_at?->format('d M Y') }}</td></tr>@empty<tr><td colspan="5" class="px-3 py-8 text-center text-gray-400">No payments recorded.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $payments->links() }}</div></div>
@endsection
