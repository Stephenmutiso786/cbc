@extends('layouts.finance')
@section('header', 'Invoices')
@section('content')
<div class="card p-6">@php($invoices = \App\Models\FeeInvoice::with('learner')->latest()->paginate(25))<h2 class="text-xl font-bold text-gray-800 mb-5">Invoices</h2><div class="overflow-x-auto"><table class="min-w-full divide-y divide-gray-200"><thead><tr><th class="px-3 py-2 text-left text-xs uppercase">Invoice</th><th class="px-3 py-2 text-left text-xs uppercase">Learner</th><th class="px-3 py-2 text-right text-xs uppercase">Total</th><th class="px-3 py-2 text-right text-xs uppercase">Balance</th><th class="px-3 py-2 text-left text-xs uppercase">Status</th></tr></thead><tbody class="divide-y">@forelse($invoices as $invoice)<tr><td class="px-3 py-2 text-sm">{{ $invoice->invoice_number }}</td><td class="px-3 py-2 text-sm">{{ $invoice->learner?->full_name }}</td><td class="px-3 py-2 text-right text-sm">{{ number_format($invoice->total_amount,2) }}</td><td class="px-3 py-2 text-right text-sm">{{ number_format($invoice->balance,2) }}</td><td class="px-3 py-2 text-sm">{{ ucfirst($invoice->status) }}</td></tr>@empty<tr><td colspan="5" class="px-3 py-8 text-center text-gray-400">No invoices found.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $invoices->links() }}</div></div>
@endsection
