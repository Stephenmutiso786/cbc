<!doctype html><html><head><meta charset="utf-8"><style>@page{size:A4 landscape;margin:10mm}body{font-family:DejaVu Sans,sans-serif;color:#172033;font-size:10px}.page{page-break-after:always}.page:last-child{page-break-after:auto}h1,h2,p{margin:0}.header{border-bottom:2px solid #166534;padding-bottom:8px;margin-bottom:10px}.title{color:#166534;font-size:17px}.meta{margin-top:3px;color:#4b5563}table{width:100%;border-collapse:collapse;margin-top:10px}th,td{border:1px solid #94a3b8;padding:6px;text-align:left;vertical-align:top}th{background:#ecfdf5;color:#14532d}.day{width:14%}.time{width:14%}.footer{margin-top:12px;border-top:1px solid #cbd5e1;padding-top:5px;color:#64748b;font-size:8px}</style></head><body>
@php($groups = $teacher ? $slots->groupBy('teacher_id') : $slots->groupBy('class_id'))
@forelse($groups as $group)
<section class="page"><header class="header"><h1 class="title">{{ $school?->name ?? config('school.name') }}</h1><p class="meta">{{ $teacher ? 'Teacher copy: '.$group->first()->teacher?->full_name : 'Class timetable: '.$group->first()->schoolClass?->name }} | Academic year {{ $version->academic_year }} | Term {{ $version->term }} | Version {{ $version->version_number }}</p></header><table><thead><tr><th class="day">Day</th><th class="time">Time</th><th>Class / Stream</th><th>Subject</th><th>Teacher</th><th>Room</th></tr></thead><tbody>@foreach($group as $slot)<tr><td>{{ ucfirst($slot->day_of_week) }}</td><td>{{ substr($slot->start_time,0,5) }}-{{ substr($slot->end_time,0,5) }}</td><td>{{ $slot->schoolClass?->name }}</td><td>{{ $slot->learningArea?->name }}@if($slot->is_double) (double)@endif</td><td>{{ $slot->teacher?->full_name }}</td><td>{{ $slot->venue ?: '-' }}</td></tr>@endforeach</tbody></table><footer class="footer">Generated {{ $generatedAt?->format('d M Y H:i') }} | Official school timetable</footer></section>
@empty <p>No timetable lessons exist in this version.</p>@endforelse
</body></html>
