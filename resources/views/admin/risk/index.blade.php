@extends('layouts.admin')
@section('header', 'At-Risk Learners')
@section('content')
<div class="space-y-5"><div><h1 class="text-2xl font-bold">At-Risk Learners</h1><p class="text-sm text-gray-500">Predictions are decision support, not automatic decisions. Review the factors with the learner and family.</p></div><form class="flex gap-2"><select name="class" class="rounded border px-3 py-2"><option value="">All classes</option>@foreach($classes as $class)<option value="{{ $class->id }}" @selected(request('class') == $class->id)>{{ $class->name }}</option>@endforeach</select><button class="rounded bg-gray-800 px-3 py-2 text-white">Filter</button></form><div class="overflow-x-auto rounded-xl bg-white shadow"><table class="min-w-full text-sm"><thead class="bg-gray-50 text-left"><tr><th class="p-3">Learner</th><th class="p-3">Class</th><th class="p-3">Risk</th><th class="p-3">Contributing factors</th><th class="p-3">Updated</th></tr></thead><tbody>@forelse($predictions as $prediction)<tr class="border-t"><td class="p-3 font-semibold"><a class="text-violet-700 hover:underline" href="{{ route('admin.risk.show', $prediction) }}">{{ $prediction->learner->full_name }}</a></td><td class="p-3">{{ $prediction->learner->schoolClass?->name }}</td><td class="p-3"><span class="rounded-full px-2 py-1 text-xs font-bold {{ $prediction->risk_level === 'high' ? 'bg-red-100 text-red-800' : ($prediction->risk_level === 'medium' ? 'bg-amber-100 text-amber-800' : 'bg-green-100 text-green-800') }}">{{ ucfirst($prediction->risk_level) }} · {{ number_format($prediction->risk_score) }}%</span></td><td class="p-3 text-gray-600">{{ collect($prediction->top_factors)->map(fn($f) => str_replace('_', ' ', $f))->implode(', ') ?: 'Model factors unavailable' }}</td><td class="p-3 text-gray-500">{{ $prediction->computed_at?->diffForHumans() }}</td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-gray-500">No predictions yet. Ask the platform administrator to train and run the model.</td></tr>@endforelse</tbody></table></div>{{ $predictions->links() }}</div>
@endsection
