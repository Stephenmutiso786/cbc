@extends('layouts.admin')
@section('header', 'Impersonate user')
@section('content')
<div class="mx-auto max-w-4xl space-y-6"><div class="rounded-xl border border-amber-200 bg-amber-50 p-5 text-sm text-amber-900"><strong>Super Admin only:</strong> impersonation is audited. Use it only to diagnose a reported access or workflow problem, and stop the session when finished.</div><div class="overflow-x-auto rounded-xl bg-white shadow-sm"><table class="min-w-full divide-y"><thead class="bg-gray-50"><tr><th class="px-5 py-3 text-left text-xs uppercase">User record</th><th class="px-5 py-3 text-left text-xs uppercase">Role</th></tr></thead><tbody class="divide-y">@foreach($users as $user)<tr><td class="px-5 py-4"><div class="flex flex-wrap items-center gap-3"><div><p class="font-semibold">{{ $user->name }}</p><p class="text-sm text-gray-500">{{ $user->email }}</p></div><form method="POST" action="{{ route('admin.impersonate.start', $user) }}" onsubmit="return confirm('Impersonate {{ addslashes($user->name) }}? This action will be audited.');">@csrf<button type="submit" class="rounded-lg bg-amber-600 px-3 py-2 text-sm font-semibold text-white hover:bg-amber-700">Impersonate</button></form></div></td><td class="px-5 py-4 text-sm">{{ $user->roles->pluck('name')->map(fn($role) => ucwords(str_replace('-', ' ', $role)))->join(', ') ?: 'No role' }}</td></tr>@endforeach</tbody></table></div></div>
@endsection
