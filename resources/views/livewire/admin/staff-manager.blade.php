<div>
    <div class="mb-6 flex flex-wrap items-center justify-between gap-3"><div><h2 class="text-xl font-bold text-gray-900">Staff register</h2><p class="text-sm text-gray-500">{{ $staff->total() }} staff records</p></div><div class="flex gap-2"><button wire:click="$set('showImport', true)" class="rounded-lg border border-green-700 px-4 py-2 text-sm text-green-700">Import CSV</button><button wire:click="create" class="rounded-lg bg-green-700 px-4 py-2 text-sm font-semibold text-white">Add staff</button></div></div>
    @if(session('success'))<div class="mb-4 rounded-lg bg-green-50 px-4 py-3 text-sm text-green-700">{{ session('success') }}</div>@endif
    <div class="overflow-x-auto rounded-xl bg-white shadow-sm"><table class="min-w-full divide-y divide-gray-200"><thead class="bg-gray-50"><tr><th class="px-4 py-3 text-left text-xs uppercase">Name</th><th class="px-4 py-3 text-left text-xs uppercase">Staff No.</th><th class="px-4 py-3 text-left text-xs uppercase">Role</th><th class="px-4 py-3 text-left text-xs uppercase">Contact</th><th></th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($staff as $member)<tr><td class="px-4 py-3 text-sm font-semibold">{{ $member->full_name }}</td><td class="px-4 py-3 text-sm">{{ $member->staff_number }}</td><td class="px-4 py-3 text-sm">{{ $member->user?->getRoleNames()->join(', ') ?: 'No role' }}</td><td class="px-4 py-3 text-sm">{{ $member->email }}<br>{{ $member->phone_number }}</td><td class="px-4 py-3 text-right"><button wire:click="edit({{ $member->id }})" class="text-sm text-green-700">Edit</button></td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-sm text-gray-400">No staff records.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $staff->links() }}</div>
    @if($showForm)<div class="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4"><div class="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-xl bg-white p-6"><h3 class="mb-4 text-lg font-bold">{{ $editingId ? 'Edit staff' : 'Add staff' }}</h3><div class="grid grid-cols-1 gap-3 md:grid-cols-2">@foreach([['staff_number','Staff number'],['first_name','First name'],['last_name','Last name'],['email','Email'],['phone_number','Phone number'],['designation','Designation']] as [$field,$label])<input wire:model="form.{{ $field }}" placeholder="{{ $label }}" class="rounded-lg border px-3 py-2 text-sm">@endforeach<select wire:model="form.gender" class="rounded-lg border px-3 py-2 text-sm"><option value="male">Male</option><option value="female">Female</option></select><select wire:model="form.staff_type" class="rounded-lg border px-3 py-2 text-sm"><option value="teaching">Teaching</option><option value="non_teaching">Non teaching</option></select><select wire:model="form.employment_type" class="rounded-lg border px-3 py-2 text-sm">@foreach(['permanent','contract','bom','volunteer'] as $type)<option value="{{ $type }}">{{ ucfirst($type) }}</option>@endforeach</select><select wire:model="form.role" class="rounded-lg border px-3 py-2 text-sm">@foreach($roles as $role)<option value="{{ $role->name }}">{{ $role->name }}</option>@endforeach</select><input wire:model="form.date_joined" type="date" class="rounded-lg border px-3 py-2 text-sm"><input wire:model="form.password" type="password" placeholder="{{ $editingId ? 'New password (optional)' : 'Password' }}" class="rounded-lg border px-3 py-2 text-sm"></div><div class="mt-5 flex justify-end gap-2"><button wire:click="$set('showForm', false)" class="rounded-lg border px-4 py-2 text-sm">Cancel</button><button wire:click="save" class="rounded-lg bg-green-700 px-4 py-2 text-sm font-semibold text-white">Save staff</button></div></div></div>@endif
    @if($showImport)<div class="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4"><div class="w-full max-w-xl rounded-xl bg-white p-6"><h3 class="mb-3 text-lg font-bold">Import staff CSV</h3><p class="mb-4 text-sm text-gray-600">Headers: staff_number, first_name, last_name, email, phone_number, gender, employment_type, staff_type, designation, date_joined, role, password. If password is omitted, use the temporary password ChangeMe@123 and change it after login.</p><input wire:model="csvFile" type="file" accept=".csv,.txt" class="w-full rounded-lg border px-3 py-2 text-sm">@error('csvFile')<span class="text-xs text-red-600">{{ $message }}</span>@enderror@if($importErrors)<div class="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">@foreach($importErrors as $error)<div>{{ $error }}</div>@endforeach</div>@endif<div class="mt-5 flex justify-end gap-2"><button wire:click="$set('showImport', false)" class="rounded-lg border px-4 py-2 text-sm">Close</button><button wire:click="importCsv" class="rounded-lg bg-green-700 px-4 py-2 text-sm font-semibold text-white">Import</button></div></div></div>@endif
</div>
