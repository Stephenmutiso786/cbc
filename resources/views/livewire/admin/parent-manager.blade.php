<div class="space-y-6">
    <div class="flex flex-wrap items-center justify-between gap-3"><div><h2 class="text-2xl font-bold text-gray-900">Parent Management</h2><p class="text-sm text-gray-500">Link parents to learners and use their phone numbers for result SMS.</p></div><button wire:click="create" class="rounded-lg bg-green-700 px-4 py-2 text-sm font-semibold text-white">Add parent</button></div>
    @if(session('success'))<div class="rounded-lg bg-green-50 px-4 py-3 text-sm text-green-700">{{ session('success') }}</div>@endif
    <div class="overflow-x-auto rounded-xl bg-white shadow-sm"><table class="min-w-full divide-y divide-gray-200"><thead class="bg-gray-50"><tr><th class="px-4 py-3 text-left text-xs uppercase">Parent</th><th class="px-4 py-3 text-left text-xs uppercase">Phone</th><th class="px-4 py-3 text-left text-xs uppercase">Linked learners</th><th class="px-4 py-3 text-right text-xs uppercase">Actions</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($parents as $parent)<tr><td class="px-4 py-3 text-sm font-semibold">{{ $parent->full_name }}<br><span class="font-normal text-gray-500">{{ $parent->email ?: 'No email' }}</span></td><td class="px-4 py-3 text-sm">{{ $parent->phone_number }}</td><td class="px-4 py-3 text-sm">{{ $parent->learners->map(fn ($learner) => $learner->full_name.' ('.$learner->schoolClass?->name.')')->join(', ') }}</td><td class="px-4 py-3 text-right"><button wire:click="edit({{ $parent->id }})" class="mr-3 text-sm text-green-700">Edit</button><button wire:click="delete({{ $parent->id }})" wire:confirm="Delete this parent record? Learners will remain." class="text-sm text-red-600">Delete</button></td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-sm text-gray-500">No parent records yet.</td></tr>@endforelse</tbody></table></div><div>{{ $parents->links() }}</div>
    @if($showForm)<div class="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4"><div class="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-xl bg-white p-6"><div class="mb-5 flex justify-between"><h3 class="text-lg font-bold">{{ $editingId ? 'Edit parent' : 'Add parent' }}</h3><button wire:click="$set('showForm', false)">&times;</button></div><div class="grid grid-cols-1 gap-3 sm:grid-cols-2"><input wire:model="firstName" placeholder="First name" class="rounded-lg border px-3 py-2 text-sm"><input wire:model="lastName" placeholder="Last name" class="rounded-lg border px-3 py-2 text-sm"><input wire:model="phoneNumber" placeholder="Results SMS phone number" class="rounded-lg border px-3 py-2 text-sm"><input wire:model="email" placeholder="Email (optional)" class="rounded-lg border px-3 py-2 text-sm"><select wire:model="relationship" class="rounded-lg border px-3 py-2 text-sm sm:col-span-2"><option value="father">Father</option><option value="mother">Mother</option><option value="guardian">Guardian</option><option value="other">Other</option></select></div><div class="mt-4"><p class="mb-2 text-xs font-semibold uppercase text-gray-500">Link learners</p><input wire:model.live.debounce.300ms="learnerSearch" placeholder="Search learner by name or admission number" class="mb-3 w-full rounded-lg border px-3 py-2 text-sm"><div class="grid max-h-64 grid-cols-1 gap-2 overflow-y-auto rounded-lg border p-3 sm:grid-cols-2">@foreach($learners as $learner)<label class="flex items-center gap-2 text-sm"><input type="checkbox" wire:model="learnerIds" value="{{ $learner->id }}" class="rounded border-gray-300"><span>{{ $learner->full_name }} <span class="text-gray-500">({{ $learner->schoolClass?->name ?? $learner->grade_level?->value ?? $learner->grade_level }})</span></span></label>@endforeach @if($learners->isEmpty())<span class="text-sm text-gray-500">No learners match that search.</span>@endif</div></div>@error('learnerIds')<p class="mt-2 text-sm text-red-600">{{ $message }}</p>@enderror<div class="mt-5 flex justify-end gap-2"><button wire:click="$set('showForm', false)" class="rounded-lg border px-4 py-2 text-sm">Cancel</button><button wire:click="save" wire:loading.attr="disabled" class="rounded-lg bg-green-700 px-4 py-2 text-sm font-semibold text-white">Save parent</button></div></div></div>@endif
</div>
