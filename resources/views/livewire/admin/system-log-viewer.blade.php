<div class="mx-auto max-w-7xl space-y-6"><div><h2 class="text-2xl font-bold text-gray-900">System logs</h2><p class="text-sm text-gray-500">Super Admin-only request activity, status codes, users, and failures. Passwords and request bodies are never recorded.</p></div><input wire:model.live.debounce.300ms="search" placeholder="Filter by path, method, or status" class="w-full rounded-lg border bg-white px-3 py-2"><div class="overflow-x-auto rounded-xl bg-white shadow-sm"><table class="min-w-full divide-y text-left text-sm"><thead class="bg-gray-50 text-xs uppercase tracking-wide text-gray-500"><tr><th class="px-4 py-3">Time</th><th class="px-4 py-3">User</th><th class="px-4 py-3">Request</th><th class="px-4 py-3">Status</th><th class="px-4 py-3">IP</th><th class="px-4 py-3">Duration</th></tr></thead><tbody class="divide-y">@forelse($logs as $log)<tr><td class="whitespace-nowrap px-4 py-3 text-xs">{{ $log->created_at?->format('d M Y H:i:s') }}</td><td class="px-4 py-3">{{ $log->user?->name ?: 'Guest' }}</td><td class="px-4 py-3"><span class="font-semibold">{{ $log->method }}</span> {{ $log->path }}</td><td class="px-4 py-3"><span class="rounded-full px-2 py-1 text-xs font-semibold {{ $log->status >= 400 ? 'bg-red-100 text-red-700' : 'bg-green-100 text-green-700' }}">{{ $log->status }}</span></td><td class="px-4 py-3 text-xs text-gray-500">{{ $log->ip_address }}</td><td class="px-4 py-3 text-xs">{{ $log->duration_ms }} ms</td></tr>@empty<tr><td colspan="6" class="p-10 text-center text-gray-500">No system logs recorded yet.</td></tr>@endforelse</tbody></table><div class="border-t bg-gray-50 px-4 py-3">{{ $logs->links() }}</div></div></div>
