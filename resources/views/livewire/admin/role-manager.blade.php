<div class="space-y-6">
    <div><h2 class="text-2xl font-bold text-gray-900">Roles and permissions</h2><p class="text-sm text-gray-500">Create custom roles and control exactly which system actions each role can perform.</p></div>
    @if($notice)<div class="rounded-lg bg-green-50 p-3 text-sm text-green-800">{{ $notice }}</div>@endif
    <section class="rounded-xl bg-white p-6 shadow-sm"><div class="mb-4 flex items-center justify-between"><h3 class="font-bold">{{ $editingId ? 'Edit role' : 'Create role' }}</h3><button wire:click="resetForm" class="text-sm text-gray-500">Clear</button></div><div class="flex gap-2"><input wire:model="name" placeholder="Role name e.g. deputy headteacher" class="flex-1 rounded-lg border px-3 py-2 text-sm"><button wire:click="save" wire:loading.attr="disabled" class="rounded-lg bg-green-700 px-4 py-2 text-sm font-semibold text-white">Save role</button></div>@error('name')<p class="mt-2 text-sm text-red-600">{{ $message }}</p>@enderror<div class="mt-5 grid max-h-72 grid-cols-1 gap-2 overflow-y-auto rounded-lg border p-4 sm:grid-cols-2 lg:grid-cols-3">@foreach($permissions as $permission)<label class="flex items-center gap-2 text-sm"><input type="checkbox" wire:model="permissionIds" value="{{ $permission->id }}" class="rounded border-gray-300 text-green-700"><span>{{ $permission->name }}</span></label>@endforeach</div></section>
    <section class="overflow-x-auto rounded-xl bg-white shadow-sm"><table class="min-w-full divide-y divide-gray-200"><thead class="bg-gray-50"><tr><th class="px-4 py-3 text-left text-xs uppercase">Role</th><th class="px-4 py-3 text-left text-xs uppercase">Permissions</th><th class="px-4 py-3 text-right text-xs uppercase">Actions</th></tr></thead><tbody class="divide-y divide-gray-100">@foreach($roles as $role)<tr><td class="px-4 py-3 font-semibold">{{ $role->name }}</td><td class="max-w-2xl px-4 py-3 text-xs text-gray-600">{{ $role->permissions->pluck('name')->join(', ') ?: 'No permissions' }}</td><td class="px-4 py-3 text-right"><button wire:click="edit({{ $role->id }})" class="mr-3 text-sm text-blue-700">Edit</button>@if(!in_array($role->name, ['admin','super-admin','headteacher']))<button wire:click="delete({{ $role->id }})" wire:confirm="Delete this role?" class="text-sm text-red-600">Delete</button>@endif</td></tr>@endforeach</tbody></table></section>
</div>
