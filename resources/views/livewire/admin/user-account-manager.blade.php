<div class="mx-auto max-w-6xl space-y-6"><div class="flex flex-wrap items-start justify-between gap-3"><div><h2 class="text-2xl font-bold text-gray-900">User accounts</h2><p class="text-sm text-gray-500">School Admins and Super Admins can create accounts and set a new temporary password. Super Admin accounts are protected.</p></div><button wire:click="create" class="rounded-lg bg-green-700 px-4 py-2 font-semibold text-white">Create user</button></div>@if($notice)<div class="rounded-lg bg-green-50 p-3 text-sm text-green-800">{{ $notice }}</div>@endif@if($showForm)<form wire:submit="save" class="grid gap-3 rounded-xl bg-white p-6 shadow-sm md:grid-cols-2"><input wire:model="name" placeholder="Full name" class="rounded-lg border px-3 py-2"><input wire:model="email" type="email" placeholder="Email" class="rounded-lg border px-3 py-2"><select wire:model="role" class="rounded-lg border px-3 py-2"><option value="">Select role</option>@foreach($roles as $item)<option value="{{ $item->name }}">{{ ucwords(str_replace('-', ' ', $item->name)) }}</option>@endforeach</select><input wire:model="password" type="password" placeholder="{{ $editingId ? 'New password (optional)' : 'Password' }}" class="rounded-lg border px-3 py-2">@if($role === 'learner')<label class="text-sm text-gray-700">Link learner record<select wire:model="learnerId" class="mt-1 w-full rounded-lg border px-3 py-2"><option value="">Select learner</option>@foreach($learners as $learner)<option value="{{ $learner->id }}">{{ $learner->full_name }} — {{ $learner->admission_number }}</option>@endforeach</select>@error('learnerId')<span class="text-xs text-red-600">{{ $message }}</span>@enderror</label>@endif<div class="flex gap-2 md:col-span-2"><button type="submit" class="rounded-lg bg-green-700 px-4 py-2 font-semibold text-white">{{ $editingId ? 'Save / reset password' : 'Create account' }}</button><button type="button" wire:click="$set('showForm', false)" class="rounded-lg border px-4 py-2">Cancel</button></div></form>@endif@if($resettingPasswordFor)<form wire:submit="setPassword" class="rounded-xl border border-amber-200 bg-amber-50 p-5 shadow-sm"><h3 class="font-bold text-amber-950">Set a new temporary password</h3><p class="mt-1 text-sm text-amber-900">The user will be required to create a personal password after signing in.</p><div class="mt-4 grid gap-3 md:grid-cols-2"><label class="text-sm font-medium">New password<input wire:model="newPassword" type="password" minlength="8" required class="mt-1 w-full rounded-lg border px-3 py-2"></label><label class="text-sm font-medium">Confirm password<input wire:model="newPasswordConfirmation" type="password" minlength="8" required class="mt-1 w-full rounded-lg border px-3 py-2"></label></div>@error('newPassword')<p class="mt-2 text-sm text-red-700">{{ $message }}</p>@enderror<div class="mt-4 flex gap-2"><button class="rounded-lg bg-amber-700 px-4 py-2 text-sm font-semibold text-white">Set password</button><button type="button" wire:click="$set('resettingPasswordFor', null)" class="rounded-lg border px-4 py-2 text-sm">Cancel</button></div></form>@endif<div class="overflow-x-auto rounded-xl bg-white shadow-sm"><table class="min-w-full divide-y text-left text-sm"><thead class="bg-gray-50 text-xs uppercase"><tr><th class="px-4 py-3">User</th><th class="px-4 py-3">Role</th><th class="px-4 py-3">Actions</th></tr></thead><tbody class="divide-y">@foreach($users as $user)<tr><td class="px-4 py-3"><strong>{{ $user->name }}</strong><p class="text-xs text-gray-500">{{ $user->email }}</p></td><td class="px-4 py-3">{{ $user->roles->pluck('name')->join(', ') ?: 'No role' }}@if($user->learner)<p class="text-xs text-gray-500">Linked: {{ $user->learner->admission_number }}</p>@endif</td><td class="px-4 py-3"><div class="flex flex-wrap gap-2">@if(!$user->hasRole('super-admin'))<button wire:click="edit({{ $user->id }})" class="text-green-700">Edit</button><button wire:click="openPasswordReset({{ $user->id }})" class="text-amber-700">Set password</button>@else<span class="text-xs font-semibold text-gray-500">Protected Super Admin</span>@endif</div></td></tr>@endforeach</tbody></table><div class="border-t bg-gray-50 px-4 py-3">{{ $users->links() }}</div></div></div>
