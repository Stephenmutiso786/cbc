<div class="space-y-6"><div class="flex items-center justify-between"><div><h1 class="text-2xl font-bold">Schools</h1><p class="text-sm text-gray-500">Separate school tenants and their administrators.</p></div><button wire:click="create" class="rounded-lg bg-green-700 px-4 py-2 text-sm font-semibold text-white">New school</button></div>@if(session('success'))<div class="rounded-lg bg-green-50 p-4 text-sm text-green-800">{{ session('success') }}</div>@endif @if($generatedPassword)<div class="rounded-lg bg-yellow-50 p-4 text-sm text-yellow-900">Admin email: <strong>{{ $adminEmail }}</strong><br>Temporary password: <strong class="font-mono">{{ $generatedPassword }}</strong></div>@endif <div class="overflow-x-auto rounded-xl bg-white shadow-sm"><table class="min-w-full text-sm"><thead><tr class="border-b text-left"><th class="p-4">School</th><th class="p-4">Users</th><th class="p-4">Learners</th><th class="p-4">Status</th><th class="p-4"></th></tr></thead><tbody>@foreach($schools as $school)<tr class="border-b"><td class="p-4"><strong>{{ $school->name }}</strong><br><span class="text-xs text-gray-500">{{ $school->type }}</span></td><td class="p-4">{{ $school->users_count }}</td><td class="p-4">{{ $school->learners_count }}</td><td class="p-4">{{ $school->is_active ? 'Active' : 'Inactive' }}</td><td class="p-4"><button wire:click="edit({{ $school->id }})" class="text-green-700">Edit</button> <button wire:click="toggleActive({{ $school->id }})" class="ml-3 text-gray-600">{{ $school->is_active ? 'Deactivate' : 'Activate' }}</button></td></tr>@endforeach</tbody></table></div>{{ $schools->links() }} @if($showForm)<div class="fixed inset-0 z-50 overflow-y-auto bg-black/50 p-4"><div class="mx-auto mt-12 max-w-lg rounded-xl bg-white p-6"><h2 class="text-lg font-bold">{{ $editingId ? 'Edit school' : 'Create school' }}</h2><form wire:submit="save" class="mt-4 space-y-3"><input wire:model="form.name" placeholder="School name" class="w-full rounded border p-2">@error('form.name')<p class="text-xs text-red-600">{{ $message }}</p>@enderror<select wire:model="form.type" class="w-full rounded border p-2"><option value="primary">Primary</option><option value="secondary">Secondary</option><option value="mixed">Mixed</option></select><input wire:model="form.motto" placeholder="Motto" class="w-full rounded border p-2"><input wire:model="form.phone" placeholder="Phone" class="w-full rounded border p-2"><input wire:model="form.email" placeholder="School email" class="w-full rounded border p-2">@if(!$editingId)<hr><input wire:model="adminName" placeholder="First admin name" class="w-full rounded border p-2"><input wire:model="adminEmail" placeholder="First admin email" class="w-full rounded border p-2">@endif<div class="flex justify-end gap-2"><button type="button" wire:click="$set('showForm', false)" class="rounded border px-4 py-2">Cancel</button><button class="rounded bg-green-700 px-4 py-2 text-white">Save</button></div></form></div></div>@endif</div>
