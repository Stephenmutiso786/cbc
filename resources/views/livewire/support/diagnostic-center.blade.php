<div class="space-y-6"><div class="flex flex-wrap items-center justify-between gap-3"><div><h2 class="text-2xl font-bold text-gray-900">System diagnostics</h2><p class="text-sm text-gray-500">Super Admin-only checks for common application, database, storage, and integration failures.</p></div><button wire:click="scan" wire:loading.attr="disabled" class="rounded-lg bg-green-700 px-4 py-2 font-semibold text-white">Scan again</button></div><div class="grid gap-3">@foreach($checks as $check)<div class="rounded-xl bg-white p-5 shadow-sm"><div class="flex items-center justify-between gap-4"><h3 class="font-semibold">{{ $check['name'] }}</h3><span class="rounded-full px-3 py-1 text-xs font-bold {{ $check['status'] === 'ok' ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-700' }}">{{ strtoupper($check['status']) }}</span></div><p class="mt-2 text-sm text-gray-600">{{ $check['message'] }}</p></div>@endforeach</div></div>
