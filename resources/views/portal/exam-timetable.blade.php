@php($layout = auth()->user()->hasRole('learner') ? 'layouts.student' : (auth()->user()->hasRole('parent') ? 'layouts.parent' : 'layouts.teacher'))
@extends($layout)
@section('header', $title)
@section('content')
<div class="mx-auto max-w-5xl"><h2 class="text-2xl font-bold text-gray-900">{{ $title }}</h2><p class="mt-1 text-sm text-gray-500">Only exam schedules published by the school are shown.</p><div class="mt-5 overflow-x-auto rounded-xl bg-white shadow-sm"><table class="min-w-full divide-y text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th class="px-4 py-3">Date</th><th class="px-4 py-3">Time</th><th class="px-4 py-3">Class</th><th class="px-4 py-3">Exam</th><th class="px-4 py-3">Subject</th><th class="px-4 py-3">Venue</th></tr></thead><tbody class="divide-y">@forelse($slots as $slot)<tr><td class="px-4 py-3">{{ $slot->date?->format('d M Y') }}</td><td class="px-4 py-3">{{ substr($slot->start_time, 0, 5) }} – {{ substr($slot->end_time, 0, 5) }}</td><td class="px-4 py-3">{{ $slot->schoolClass?->name }}</td><td class="px-4 py-3">{{ $slot->exam?->name }}</td><td class="px-4 py-3">{{ $slot->exam?->learningArea?->name }}</td><td class="px-4 py-3">{{ $slot->venue }}</td></tr>@empty<tr><td colspan="6" class="px-4 py-10 text-center text-gray-500">No published exam timetable is available.</td></tr>@endforelse</tbody></table></div></div>
@endsection
