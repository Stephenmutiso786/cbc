@extends('layouts.student')
@section('content')
<div class="mx-auto max-w-6xl"><h2 class="text-2xl font-bold text-gray-900">Learning Notes</h2><p class="mt-1 text-sm text-gray-500">Published resources for {{ $learner->grade_level?->value ?? $learner->grade_level }}.</p><div class="mt-6 grid gap-4 md:grid-cols-2">@forelse($notes as $note)<article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-xs font-semibold uppercase tracking-wide text-emerald-700">{{ $note->learningArea?->name ?: 'Learning area' }}</p><h3 class="mt-1 text-lg font-bold text-gray-900">{{ $note->title }}</h3><p class="mt-2 text-sm text-gray-500">{{ $note->description }}</p>@if($note->external_url)<a class="mt-4 inline-block text-sm font-semibold text-blue-700 underline" href="{{ $note->external_url }}" target="_blank" rel="noopener">Open resource</a>@elseif($note->file_path)<a class="mt-4 inline-block text-sm font-semibold text-emerald-700 underline" href="{{ route('files.notes', $note) }}">Open resource</a>@endif</article>@empty<div class="rounded-xl bg-white p-10 text-center text-gray-500 md:col-span-2">No learning notes have been published for your grade.</div>@endforelse</div></div>
@endsection
